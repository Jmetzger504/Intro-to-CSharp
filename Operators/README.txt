This just goes over how we can override the Object.Equals() method to compare the fields of a two Robot class objects to determine their equality.
The "==" comparison operator will only check if the Robot objects references (place in memory) are equal, though we could overload that to do the same thing.
